# Now set up the programs and science stuff

# # add datalad and friends
# PATH=${HOME}/.local/bin:${PATH}
# PATH=/usr/local/sbin:${PATH}

# add spikecheck/spikefix
PATH=${HOME}/code/spikefix:${PATH}
export PATH

# add local directory
PATH=.:${PATH}
export PATH


# add ukbb pipeline
export BB_BIN_DIR=/home/frederic/code/UK_biobank_pipeline_v_1-master

# slurm helpers
alias slacct='sacct --units=G --format JobName%15,JobID,TimelimitRaw,TotalCPU,MaxRSS,MaxVMSize,MaxPages,Elapsed,State,Nodelist --starttime now-24hours'
alias scancelall='scancel -u frederic'
alias scancelpending='scancel -u frederic -t PENDING'

unset DBUS_SESSION_BUS_ADDRESS

conda activate mic
